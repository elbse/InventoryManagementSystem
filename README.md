# 🛒 Inventory Management System
*A simple Python program for tracking items in a small shop or store.*

## 📌 Description
This Inventory Management System allows users to manage shop items using **Object-Oriented Programming (OOP)** principles.  
The program can:

- Add new items  
- Update quantities  
- Display all inventory items  
- Show total inventory value  
- Handle user input safely using `try` and `except`

The system uses two classes:  
- **InventoryItem** – represents a single store item  
- **InventoryManager** – manages all items and system operations  

---

## 📦 Features
### ✔ Add Item
Allows the user to input the item name, quantity, and price.

### ✔ Update Item Quantity
Lets the user change the quantity of an existing item.

### ✔ Display Items
Shows all inventory items including:  
- Name  
- Quantity  
- Price (₱ Peso)  
- Total value (quantity × price)

### ✔ Calculate Total Inventory Value
Computes the total worth of all items in the inventory.

### ✔ User Menu
Interactive menu for selecting actions.

### ✔ Error Handling
Uses `try-except` to prevent program crashes when invalid input is entered.

---

## 🧱 OOP Concepts Used
### **1. Classes & Objects**
- `InventoryItem` is a blueprint for each item.  
- `InventoryManager` manages a list of item objects.

### **2. Encapsulation**
Item properties (`name`, `quantity`, `price`) are stored inside the `InventoryItem` class.

### **3. Methods**
The system uses methods such as:  
- `add_item()`  
- `update_quantity()`  
- `display_items()`  
- `calculate_total_inventory_value()`  

---

## ▶ How to Run the Program
1. Install Python (version 3 recommended).  
2. Save the program as `inventory_management_system.py`.  
3. Run the file using: `python app.py`.

   
---

## 👨‍💻 Charisse Priego



